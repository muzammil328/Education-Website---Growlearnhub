# @muzammil328/education-packages

Shared TypeScript types, enums, and Zod validation schemas for the education platform.

Used by both `backend` and `frontend` to ensure type safety across the stack.